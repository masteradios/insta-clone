import 'package:flutter/material.dart';

class WebLayout extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Scaffold(body: Center(child: Text('Welcome to Web')),));
  }
}
